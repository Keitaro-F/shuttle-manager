# LINEシャトル報告連携 実装計画

## 1. この文書の目的

本書は、LINEシャトル報告連携をフェーズ単位で実装するための進行管理文書である。

各Codexタスクは、作業開始前に次のファイルを読む。

1. `AGENTS.md`
2. `docs/line-mvp.md`
3. `docs/line-implementation-plan.md`

機能要件の詳細は `docs/line-mvp.md` を正とし、本書では実装順、依存関係、完了条件、現在地を管理する。

## 2. 現在の開発状態

最終更新日：2026年8月11日

| 項目 | 現在の状態 |
| --- | --- |
| リポジトリ | `/Users/user/dev/shuttle-manager` |
| 作業ブランチ | `feature/line-integration` |
| 現在地 | フェーズ9完了（誤報告の削除改善・購入管理・拠点間移動） |
| 次の作業 | なし（フェーズ10以降は未着手） |
| 既存DB | Neon PostgreSQL（開発用）。既存SQLiteデータは移行していない |
| LINEメッセージ訂正 | 編集機能には依存せず、送信取消・再送またはWeb編集を利用 |

## 3. 進行ルール

- 同じローカルプロジェクト内で、フェーズごとに新しいCodexタスクを作成する
- 原則としてフェーズを順番に進め、同じファイルを複数タスクで同時編集しない
- MVP完了までは `feature/line-integration` ブランチを使用する
- 各フェーズ開始時に `git status` を確認する
- 各フェーズでは対象外の機能へ着手しない
- 各フェーズ終了時にLint、型チェック、必要なテスト、production buildを実行する
- Codexには原則としてコミットさせず、利用者が差分を確認してからコミットする
- 仕様変更が発生した場合は、コードより先に `docs/line-mvp.md` または本書を更新する
- 秘密情報をコード、Markdown、Git履歴へ記録しない
- PostgreSQL移行時に既存SQLiteデータは引き継がない

## 4. フェーズ一覧

| フェーズ | 内容 | 状態 | 目安 |
| --- | --- | --- | ---: |
| 1 | 調査・MVP仕様・実装計画 | 完了 | 完了済み |
| 2 | PostgreSQL・Prismaデータモデル | 完了 | 3〜6時間 |
| 3 | メッセージ解析・Report保存処理 | 完了 | 3〜5時間 |
| 4 | LINE公式アカウント・Webhook準備 | 完了 | 1〜3時間 |
| 5 | Webhook受信・署名・グループ制限 | 完了 | 4〜7時間 |
| 6 | LINE返信・残量確認・送信取消 | 完了 | 3〜5時間 |
| 7 | 結合テスト・本番デプロイ | 完了 | 3〜6時間 |
| 8 | MVP運用確認・改善判断 | 完了 | 1〜2週間の試験運用 |
| 9 | 誤報告の削除改善・購入管理・拠点間移動 | 完了 | 8〜14時間 |
| 10 | 使用量グラフ・在庫警告 | MVP後 | 6〜12時間 |
| 11 | Web認証・権限管理 | MVP後 | 4〜8時間 |

## 5. 実装順と依存関係

```text
フェーズ1：仕様確定
    ↓
フェーズ2：DB基盤
    ↓
フェーズ3：LINEに依存しない解析・保存処理
    ↓
フェーズ4：LINE公式アカウント準備
    ↓
フェーズ5：Webhook接続
    ↓
フェーズ6：返信・残量確認・送信取消
    ↓
フェーズ7：結合テスト・デプロイ
    ↓
フェーズ8：試験運用
    ↓
フェーズ9以降：削除改善・購入・グラフ・認証
```

フェーズ3まではLINE Developers Consoleの設定がなくてもローカルで実装・テストできる。フェーズ4で取得するChannel secretとChannel access tokenは、フェーズ5以降の実機確認に必要となる。

## 6. フェーズ別計画

### フェーズ1：調査・MVP仕様・実装計画

状態：**完了**

実施済み：

- 現在のNext.js、Prisma、SQLite構成を調査
- 既存画面、API、入力値検証を確認
- Lint、TypeScript、production build、Prisma migrationを確認
- `feature/line-integration` ブランチを作成
- `docs/line-mvp.md` を作成
- LINEメッセージ編集には依存しない方針へ修正
- 既存SQLiteデータは移行不要と決定
- 本実装計画を作成

完了条件：

- [x] MVPの目的と対象範囲が文書化されている
- [x] LINE報告形式が決まっている
- [x] 訂正方法が決まっている
- [x] フェーズと実装順が決まっている

### フェーズ2：PostgreSQL・Prismaデータモデル

状態：**完了**

目的：

外部公開環境でLINE Webhookから永続的にデータを保存できるDB基盤を作る。

PostgreSQL移行方針：

- マネージドPostgreSQLにはNeonを使用する
- アプリ実行時は `DATABASE_URL`、migration実行時は `DIRECT_URL` を使用する
- Neonではアプリ用にpooled接続、migration用にdirect接続を設定する
- 開発用DBは本番用とは別のNeon projectまたはbranchとして作成する
- 開発用Neon projectは `shuttle-manager-dev`（PostgreSQL 17、Singapore）を使用する
- Neon Authは使用しない
- 既存SQLiteデータは移行しない
- 既存SQLite migrationは `prisma/migrations/` に保存したまま残す
- PostgreSQL migrationは `prisma/migrations-postgresql/` で新しく管理する

実装内容：

1. 使用するマネージドPostgreSQLを決定する
2. 開発用PostgreSQLデータベースを作成する
3. `DATABASE_URL` をローカル環境変数へ設定する
4. Prisma datasourceをSQLiteからPostgreSQLへ変更する
5. SQLite用migrationをPostgreSQLへ適用しないよう整理する
6. PostgreSQL用の初期migrationを作成する
7. `ReportSource` enumを追加する
8. `Report` にLINE連携用フィールドと `reportedAt`、`updatedAt` を追加する
9. `WebhookReceipt` モデルを追加する
10. ホームと履歴の並び順を `reportedAt` 基準へ変更する
11. 空のPostgreSQL上で既存画面が動作することを確認する

注意事項：

- 現在のSQLite migrationにはSQLite専用SQLが含まれるため、そのままPostgreSQLへ適用しない
- 既存の `prisma/dev.db` のデータは移行しない
- migration履歴やSQLiteファイルを削除・移動する前に、対象を確認する
- DB接続情報をGitへコミットしない

想定変更箇所：

```text
prisma/schema.prisma
prisma/migrations/
prisma.config.ts
src/app/page.tsx
src/app/history/page.tsx
src/app/api/report/route.ts
src/app/api/report/[id]/route.ts
```

完了条件：

- [x] ローカルPostgreSQLへ接続できる
- [x] Neonの開発DBへ接続できる
- [x] PostgreSQL用migrationが適用できる
- [x] LINE連携用モデルがPrisma schemaに存在する
- [x] 空DBでWebフォームからReportを登録できる
- [x] ホームと履歴画面でReportを確認できる
- [x] Web編集機能が引き続き動作する
- [x] 型チェック、Lint、production buildが成功する

### フェーズ3：メッセージ解析・Report保存処理

状態：**完了**

目的：

LINE APIへ接続する前に、純粋なメッセージ解析と、Web・LINE共通の保存処理を完成させる。

実装内容：

1. テスト環境としてVitestを導入する
2. `src/lib/line/parse-message.ts` を作成する
3. NFKC正規化と空白・文末表現の処理を実装する
4. `report`、`status`、`invalid-report`、`ignore` の分類を実装する
5. 0以上・0.5刻みの検証を実装する
6. パーサーの単体テストを作成する
7. `src/lib/report-service.ts` を作成する
8. 前回報告の取得、保存、差分計算を共通化する
9. 既存のWeb APIを共通サービス経由へ変更する
10. Web画面の回帰テストを行う

完了条件：

- [x] 仕様書に記載した正常形式を解析できる
- [x] 全角数字と全角空白を処理できる
- [x] 不正な数値を拒否できる
- [x] 一般会話を `ignore` に分類できる
- [x] WebとLINEで利用できる保存サービスが存在する
- [x] パーサーの単体テストが成功する
- [x] 既存Web機能が壊れていない

### フェーズ4：LINE公式アカウント・Webhook準備

状態：**完了**

目的：

LINEグループからWebhookを受け取るためのLINE側設定を完了する。

利用者が行う作業：

1. LINE公式アカウントを作成する
2. Messaging APIを有効化する
3. Channel secretを取得する
4. Channel access tokenを発行する
5. Botのグループ参加を許可する
6. 自動応答メッセージを無効化する
7. Webhook利用とWebhook再送は、公開Webhook URLを設定するフェーズ7で有効化する
8. 秘密情報をローカル環境変数へ設定する

必要な環境変数：

```env
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
LINE_ALLOWED_GROUP_ID=
APP_BASE_URL=
```

完了条件：

- [x] Messaging API channelが作成されている
- [x] Channel secretとaccess tokenを取得している
- [x] 秘密情報がGit管理外に保存されている
- [x] Botをグループへ参加させられる設定になっている

### フェーズ5：Webhook受信・署名・グループ制限

状態：**完了**

目的：

LINE Platformから安全にWebhookを受信し、有効な報告だけを保存する。

実装内容：

1. `@line/bot-sdk` を追加する
2. `src/app/api/line/webhook/route.ts` を作成する
3. 未加工の本文を使った `x-line-signature` 検証を実装する
4. `events: []` の検証リクエストへHTTP 200を返す
5. イベント種別と `groupId` の判定を実装する
6. 許可していないグループと1対1トークを無視する
7. `webhookEventId` による二重登録防止を実装する
8. ReportとWebhookReceiptをDB transactionで保存する
9. 署名、グループ制限、再送の自動テストを作成する

完了条件：

- [x] 不正署名をHTTP 401で拒否できる
- [x] LINEのWebhook URL検証に成功する
- [x] 許可グループからの報告だけを保存できる
- [x] 同じWebhookを再送しても二重登録されない
- [x] DB保存失敗時にHTTP 500を返せる

### フェーズ6：LINE返信・残量確認・送信取消

状態：**完了**

目的：

LINEだけで登録結果の確認と誤報告の取消ができる状態を作る。

実装内容：

1. 登録成功時の返信を実装する
2. 前回差分の表示を実装する
3. 不正な報告への入力例返信を実装する
4. 一般会話には返信しないようにする
5. `シャトル残量` コマンドを実装する
6. `unsend` イベントで対象Reportを削除する
7. 送信取消後の正しい再送を確認する
8. LINE APIへの返信失敗をログへ記録する

完了条件：

- [x] 正常登録後に今回値と前回差が返信される
- [x] 不正な報告にだけ入力例が返信される
- [x] 一般会話を無視できる
- [x] `シャトル残量` で両拠点の最新値を取得できる
- [x] 送信取消されたReportが削除される
- [x] 取消後に再送した正しいReportだけが有効になる

### フェーズ7：結合テスト・本番デプロイ

状態：**完了**

目的：

本番相当環境でMVP全体を検証し、実際のLINEグループで利用可能にする。

実装・確認内容：

1. 本番用PostgreSQLを用意する
2. デプロイ先へ環境変数を設定する
3. `prisma migrate deploy` を実行できる構成にする
4. Next.jsアプリをデプロイする
5. LINE Developers Consoleへ本番Webhook URLを設定する
6. Webhook利用とWebhook再送を有効化する
7. Webhook URLのVerifyを実行する
8. 対象グループIDを取得して許可リストへ設定する
9. 実際のグループで報告、残量確認、取消、再送をテストする
10. ログに秘密情報が出ていないことを確認する
11. 既存Web機能の回帰テストを行う

完了条件：

- [x] `docs/line-mvp.md` の受け入れ条件をすべて満たす
- [x] 本番環境でReportが永続保存される
- [x] LINEの実グループで一連の操作が成功する
- [x] Lint、型チェック、自動テスト、production buildが成功する
- [x] READMEへセットアップと運用方法を記載している

### フェーズ8：MVP運用確認・改善判断

状態：**完了**

目的：

実際の練習で試験運用し、追加機能へ進む前にLINE入力の有効性を確認する。

確認項目：

- 報告者が従来より面倒と感じないか
- 誤認識や未登録が発生していないか
- 返信内容が分かりやすいか
- 数え間違いの発見に前回差分が役立つか
- 一般会話へ誤反応していないか
- 会計係がWeb履歴を確認しやすいか
- `0.5` の表現を今後も維持するか

フェーズ8の結果を基に、購入管理、グラフ、認証の優先順位を決める。

試験運用の結果：

| 確認項目 | 結果 |
| --- | --- |
| 報告者の負担 | 従来と報告方法が変わらず、面倒には感じない |
| 登録失敗・未登録 | 試験運用では指摘なし |
| 返信内容 | 分かりやすい |
| 前回差分 | 数え間違いの確認に役立つ |
| 一般会話への誤反応 | 発生していない |
| Web画面 | 確認できるが、場面によって動作が重く感じられる |
| `0.5` の表現 | 今後も維持する |

改善判断：

1. 現在の通常報告形式、登録時の返信構成、`0.5` の表現を維持する
2. 動的なWeb画面に軽量な読み込み表示を追加し、待機中であることを明確にする
3. 実測データがない状態でキャッシュによるデータ鮮度の変更は行わず、待ち時間が続く場合は本番計測後に原因を切り分ける
4. 報告忘れ通知は、グループ全員分のPush通数を消費して無料プランの上限へ早期に到達する可能性が高いため、現構成では実装を見送る
5. 少人数の通知先やLINE以外の通知手段を採用できる場合に限り、報告忘れ通知を再検討する
6. 購入管理、グラフ、認証は今回着手せず、次フェーズ開始時に改めて優先順位を判断する

利用制限の確認（2026年8月11日時点）：

- 現在のBot返信はMessaging APIのReply APIを使用しており、LINE公式アカウントの月間メッセージ通数にはカウントされない
- Reply APIのレート制限はチャネル単位で2,000リクエスト/秒であり、現在の部活動での利用規模では到達しにくい
- 将来の報告忘れ通知はPush APIを使用するため月間メッセージ通数にカウントされ、グループ宛てでは送信対象人数分を消費する
- LINE公式アカウントのコミュニケーションプランは月200通までだが、現在のReply APIだけの運用ではこの枠を消費しない
- Vercel Hobbyを使用する場合は月100万Function invocations、Active CPU 4時間などの枠があり、Neon Freeを使用する場合はprojectごとに月100 CU-hours、0.5 GB storageの枠がある。実際の利用状況は各管理画面で確認する

参照：

- [LINE Developers：Messaging APIの料金](https://developers.line.biz/ja/docs/messaging-api/pricing/)
- [LINE Developers：Messaging APIリファレンス](https://developers.line.biz/ja/reference/messaging-api/)
- [LINE公式アカウント料金プラン](https://www.lycbiz.com/jp/service/line-official-account/plan/)
- [Vercel Hobby Plan](https://vercel.com/docs/plans/hobby)
- [Neon Pricing](https://neon.com/pricing)

完了条件：

- [x] 試験運用の意見を確認項目ごとに記録している
- [x] 現在のLINE入力方式が有効であることを確認している
- [x] `0.5` の表現を維持すると決定している
- [x] Web画面の待機表示を追加している
- [x] 報告忘れ通知を現構成では見送る理由と、再検討条件を記録している
- [x] 現在の構成に関係する主な利用制限を確認している

### フェーズ9：誤報告の削除改善・購入管理・拠点間移動（MVP後）

状態：**完了**

主な内容：

- 送信取消期限後もLINEからDB上の誤報告を削除できる引用返信コマンド
- Web履歴からのReport削除方法と、削除権限の決定
- `シャトル1箱購入しました。` の解析
- Purchaseモデル
- 箱当たり筒数の保存
- メッセージ内での豊中・吹田への筒数配分
- 拠点間のシャトル移動記録
- 購入・移動履歴表示

LINEからの削除方針：

1. 誤った報告メッセージを長押ししてLINEの「リプライ」を選び、`シャトル報告削除`、`報告削除`、`削除` のいずれかを送信する
2. Webhookの `quotedMessageId` とReportの `lineMessageId` を照合して、引用元のReportを特定する
3. 許可済みLINEグループ内では、報告者本人に限定せず、グループメンバーが引用したLINE報告を削除できるようにする
4. Reportの `lineGroupId` がコマンド送信元の許可済みグループIDと一致することを必須とし、他グループのReportは削除しない
5. 引用元がない場合、Web報告、削除済みの報告は削除せず、操作方法または削除できない理由を返信する
6. WebhookReceiptとReport削除を同じtransactionで処理し、Webhook再送で削除処理と返信を重複させない
7. Reply APIで削除結果を返信し、LINE上の元メッセージ自体は残ることを案内する
8. 削除後は、次に新しいReportを各画面と `シャトル残量`・`残量` の最新値として扱う

Web履歴からの削除方針：

1. 誤操作を避けるため、削除ボタンは履歴一覧ではなく編集画面に置き、確認操作を必須にする
2. `DELETE /api/report/[id]` を追加し、存在しないReportにはHTTP 404を返す
3. 最新Reportを削除した場合は、次に新しいReportを最新値として表示する
4. 認証なしの公開画面に削除機能を追加しない。フェーズ9開始時に、削除に必要な最小限のアクセス制御を先行実装するか、Web削除をフェーズ11まで延期するか決定する

決定：WebからのReport削除はフェーズ11まで延期する。フェーズ9では、未認証の削除APIと削除UIを追加しない。

購入管理の方針：

1. シャトルは商品によらず1箱10筒として扱い、購入記録ごとに箱当たり筒数も保存する
2. 共通倉庫は設けず、保管場所は豊中・吹田だけとする
3. `シャトル1箱購入しました。豊中6筒、吹田4筒です。` のように、箱数と拠点別配分を同じメッセージへ記載する
4. 複数箱と、一方の拠点だけへの全量配分も許可する
5. 拠点配分を省略した場合は、購入した全筒を豊中へ配分する
6. 先頭の `シャトル`、配分の `筒`、拠点間の区切り、文末の `。` は省略可能とする
7. 配分は1筒単位の正の整数とし、合計が `箱数 × 10筒` と一致する場合だけ保存する
8. 配分した筒数を各拠点のニュー残量へ加算する
9. Web画面で購入日時、箱数、合計筒数、拠点別配分、購入直後の両拠点残量を新しい順に表示する

拠点間移動の方針：

1. `豊中から吹田へニュー2筒セミ1筒移動しました。` の形式で、ニュー・セミの片方または両方を記録する
2. `吹田にセミ2筒移動しました。` のように移動先だけが指定された場合は、もう一方の拠点を移動元として補完する
3. 先頭の `シャトル`、数量後の `筒`、末尾の `しました`、文末の `。` は省略可能とする
4. ニュー・セミを明示しない従来形式はニュー移動として扱う
5. 移動元と移動先は異なる拠点とし、指定する各移動量は0より大きい0.5筒刻みとする
6. 移動元のニュー・セミのいずれかが不足する場合は保存せず、各種類を対応する残量間で移動する
7. LINEイベントの日時、メッセージID、グループID、ユーザーID、元メッセージを保存する
8. 購入・移動メッセージの送信取消時には対応する記録を削除する
9. Report自体は書き換えず、最新Reportより後の購入・移動から現在残量を算出する
10. `/history` へ報告・購入・移動履歴を統合し、3種類を時系列で表示する「全て」と、種類・拠点のフィルターを設ける
11. 報告・購入・全ては全体・豊中・吹田で絞り込み、移動は常に両拠点に関係するため拠点フィルターを選択不可にする
12. シャトル報告は拠点・ニュー・セミ・登録元・日時、購入と移動は操作内容・登録元・日時・操作直後の両拠点残量を項目名付きで表示する
13. Webのページ遷移、履歴フィルター、登録・更新処理では、完了まで読み込み中または処理中であることを画面へ表示する
14. 購入・移動履歴のDB取得列を表示・計算に必要な項目へ限定する

現在残量の表示方針：

1. 拠点ごとの最新Reportを基準とし、それ以降の購入と移動を反映する
2. 通常報告・購入・移動の成功返信へ、処理後の豊中・吹田のニュー・セミ残量を追加する
3. `シャトル残量`・`残量` とWebホームでも同じ共通計算を使用する
4. Report、購入、移動の削除後は、残っている履歴から自動的に再計算する
5. 通常報告の前回比は、直前Report以降かつ今回の報告日時以前の購入・移動を反映した残量を基準に計算する

削除改善のテスト方針：

- 許可済みグループのメンバーが、本人または他のメンバーのLINE報告へ引用返信すると対象Reportを削除する
- 引用元がない場合、未許可グループ、別グループのReportでは削除しない
- Webhook再送で削除処理と返信を重複させない
- Web削除を実装する場合は、未認証の削除、存在しないReport、二重操作、最新値の切り替わりを確認する

購入管理へ着手する前に必要だった削除権限、1箱の筒数、購入品の保管場所、共通倉庫の方針は決定済みである。

完了条件：

- [x] 送信取消の期限後も、許可済みグループのメンバーが誤報告への引用返信からDB上のReportを削除できる
- [x] 報告者以外のグループメンバーでも引用したReportを削除できる
- [x] 引用元がない場合、未許可グループ、別グループのReportでは削除されない
- [x] 削除コマンドのWebhook再送で処理と返信が重複しない
- [x] Web履歴削除のアクセス制御方針が決定している
- [x] Web削除がフェーズ11へ延期され、未認証の削除API・UIが追加されていない
- [x] 購入管理の前提事項が決まり、購入記録と履歴を利用できる
- [x] 購入した筒をメッセージ内で豊中・吹田へ任意配分できる
- [x] 拠点間移動をLINEから記録し、Web履歴で確認できる
- [x] Lint、型チェック、自動テスト、production buildが成功する

### フェーズ10：使用量グラフ・在庫警告（MVP後）

状態：**MVP後**

主な内容：

- 拠点別残量推移
- 月別概算使用量
- 購入量
- 急激な増減の警告
- 在庫下限警告

`0.5` は実際の半筒ではないため、使用量は概算と表示する。

### フェーズ11：Web認証・権限管理（MVP後）

状態：**MVP後**

主な内容：

- 閲覧者と編集者の権限分離
- 会計係だけが履歴を編集できる認証
- 公開APIの保護

公開範囲によっては、MVP公開前に優先度を繰り上げる。

## 7. 現在地

```text
[完了] フェーズ1：調査・MVP仕様・実装計画
   ↓
[完了] フェーズ2：PostgreSQL・Prismaデータモデル
   ↓
[完了] フェーズ3：メッセージ解析・Report保存処理
   ↓
[完了] フェーズ4：LINE公式アカウント・Webhook準備
   ↓
[完了] フェーズ5：Webhook受信・署名・グループ制限
   ↓
[完了] フェーズ6：LINE返信・残量確認・送信取消
   ↓
[完了] フェーズ7：結合テスト・本番デプロイ
   ↓
[完了] フェーズ8：MVP運用確認・改善判断
   ↓
[完了] フェーズ9：誤報告の削除改善・購入管理・拠点間移動
```

## 8. フェーズ7進行メモ

2026年8月11日時点で完了している作業：

- `prisma migrate deploy` とVercel build用npm scriptsの追加
- 空のPostgreSQL 17に対する本番migrationの適用確認
- LINEグループIDを取得する一時的な発見モードと自動テストの追加
- Web登録・ホーム反映・履歴・絞り込み・編集の回帰確認
- 履歴画面でLINEとWebの登録元を識別できる表示の追加
- Lint、型チェック、自動テスト、production buildの成功
- READMEへのセットアップ・デプロイ・運用手順の記載
- 本番用Neon projectの作成とmigration適用
- Vercelへの環境変数設定とProduction deployment
- LINE Developers ConsoleへのWebhook URL設定とVerify
- Webhook利用・Webhook再送の有効化
- LINE公式アカウントのグループ参加が有効、自動応答が無効であることの確認
- テストグループIDの取得と本番許可設定
- 実グループでの通常報告・全角報告・不正入力・一般会話・残量確認の結合テスト
- 送信取消で誤報告が削除され、訂正版だけが保存されることの確認
- 本番NeonでのReport永続化とWeb画面への反映確認
- 本番ログでWebhookのHTTP 200、エラー・5xx・既知の秘密情報露出がないことの確認

残っている作業：

- なし（フェーズ8の結果は次節に記録）

## 9. フェーズ8進行メモ

2026年8月11日時点で完了している作業：

- 試験運用フィードバックの整理
- 通常報告形式、返信構成、`0.5` 表現を維持する判断
- 一般会話への誤反応がなく、登録失敗・未登録の指摘もないことの確認
- 動的なWeb画面への読み込み表示の追加
- 現行のReply APIと、将来のリマインダーで使うPush APIの通数差の確認
- 報告忘れ通知を現構成では見送る判断

フェーズ8では着手していない作業：

- 報告忘れ通知の要件定義・実装
- キャッシュやDB構成を変更する性能改善
- 購入管理、グラフ、在庫警告、認証
- LINE引用返信コマンドとWeb履歴からのReport削除（フェーズ9で実装方針に沿って検討）

## 10. フェーズ9進行メモ

2026年8月11日時点で完了している作業：

- LINE報告への引用返信による送信取消期限後のReport削除
- 許可グループ・引用元グループの照合とWebhook再送時の重複防止
- WebからのReport削除を認証追加後のフェーズ11へ延期する決定
- 1箱10筒、共通倉庫なし、メッセージ内で豊中・吹田へ任意配分する購入仕様の確定
- Purchase、PurchaseAllocation、ShuttleTransferモデルとPostgreSQL migrationの追加
- LINEからの購入配分・拠点間移動の解析、保存、結果返信、不正入力返信
- 購入・移動メッセージの送信取消による対応記録の削除
- 購入・移動履歴画面とホーム導線の追加
- 購入先省略時の豊中への全量配分と、`シャトル`・`筒`・区切り・文末句点を省略できる購入表記の追加
- 移動元・`シャトル`・`筒`・`しました`・文末句点を省略できる移動表記の追加
- ニュー・セミの片方または両方を同時に移動でき、種類省略時はニューとして扱う移動表記と残量計算を追加
- `削除`・`報告削除`による引用削除と、`残量`による最新Report取得の追加
- 報告・購入・移動履歴の `/history` への統合、「全て」の時系列表示、種類・拠点フィルターと移動時の拠点選択無効化を追加
- 最新Report以降の購入・移動を反映する現在残量の共通計算を追加
- 通常報告の前回比へ、直前Report以降の購入・移動を反映する計算を追加
- 通常報告・購入・移動のBot返信とWebホームへ共通の現在残量を表示
- 履歴カードを項目名付きの表示へ統一し、購入・移動履歴へ各操作直後の両拠点残量を表示
- ページ遷移・履歴フィルターへ読み込み表示、Web登録・更新へ処理中表示と二重送信防止を追加
- 購入・移動履歴のDB取得を必要な列だけに限定
- 空のPostgreSQL 17への既存・新規migrationの連続適用確認
- Lint、型チェック、自動テスト117件、production buildの成功

フェーズ9では着手していない作業：

- 使用量グラフ、在庫下限警告などのフェーズ10機能
- Web認証、権限管理、WebからのReport削除などのフェーズ11機能

## 11. フェーズ終了時の共通チェック

各フェーズの最後に次を確認する。

```bash
git status --short --branch
git diff --check
npm run lint
npx tsc --noEmit --incremental false
npm run build
```

自動テスト導入後は、テストコマンドも実行する。

差分と検証結果に問題がなければ、利用者が対象ファイルをステージしてコミットする。

## 12. 決定事項

| 日付 | 決定 |
| --- | --- |
| 2026-08-07 | 従来と同じLINE報告形式を維持する |
| 2026-08-07 | 数値は0以上・0.5刻みとする |
| 2026-08-07 | LINE一般会話には反応しない |
| 2026-08-07 | WebフォームはMVPでも残す |
| 2026-08-07 | LINEメッセージ編集機能には依存しない |
| 2026-08-07 | 誤報告は送信取消・再送、またはWeb履歴から修正する |
| 2026-08-07 | 既存SQLiteデータはPostgreSQLへ移行しない |
| 2026-08-07 | マネージドPostgreSQLにはNeonを使用する |
| 2026-08-07 | SQLite migrationを保持し、PostgreSQL migrationを別ディレクトリで管理する |
| 2026-08-07 | MVP完成前に購入管理とグラフを実装しない |
| 2026-08-11 | 現在の通常報告形式と返信構成を維持し、前回比は直前Report以降の購入・移動を反映する |
| 2026-08-11 | `0.5` の表現を今後も維持する |
| 2026-08-11 | 動的なWeb画面には読み込み中であることが分かる表示を行う |
| 2026-08-11 | 報告忘れ通知はグループ全員分のPush通数を消費するため現構成では見送り、少人数の通知先またはLINE以外の手段を採用できる場合に再検討する |
| 2026-08-11 | 送信取消期限後のLINE削除とWeb履歴削除はフェーズ9へ追加する。LINEでは許可済みグループ内のメンバー全員が引用返信で削除できるようにし、Webではアクセス制御方針を決めてから実装する |
| 2026-08-11 | WebからのReport削除はフェーズ11へ延期し、フェーズ9では未認証の削除API・UIを追加しない |
| 2026-08-11 | シャトルは1箱10筒、共通倉庫なしとし、購入メッセージ内で豊中・吹田へ筒数を任意配分する |
| 2026-08-11 | 未割当だった拠点間移動記録をフェーズ9へ追加し、LINE入力と購入・移動履歴表示を実装する |
| 2026-08-11 | 購入先省略時は全筒を豊中へ配分し、購入・移動メッセージの省略表記を許可する |
| 2026-08-11 | 引用削除では `削除`・`報告削除`、残量確認では `残量` も許可する |
| 2026-08-11 | Web履歴を `/history` へ統合し、種類と拠点で絞り込めるようにする |
| 2026-08-11 | 購入はニュー残量へ、移動は指定されたニュー・セミ残量へ反映し、通常報告・各イベント返信・残量コマンド・Webホームで共通の現在残量を表示する |
| 2026-08-11 | 移動ではニュー・セミの片方または両方を指定でき、どちらも明示しない従来形式はニューとして扱う |
